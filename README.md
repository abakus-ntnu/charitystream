# Abakus charity~~stream~~

## Setup

```sh
$ yarn # Install
$ yarn dev # Run dev server
```

This project uses `prettier` and `eslint`.

```sh
$ yarn lint # Check linting
$ yarn prettier # Format all files with prettier
```
